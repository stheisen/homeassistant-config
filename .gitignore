# Ignore folders.
.*

!.gitignore

# Ensure these YAML files are ignored, otherwise your secret data/credentials will leak.
ip_bans.yaml
secrets.yaml
known_devices.yaml

#ignore log and db files
*.log*
*.db*

# ignore .HA_VERSION
.HA_VERSION