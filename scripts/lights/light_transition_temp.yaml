alias: Light tranistion color temperature over time
sequence:
  - service: light.turn_on
    data_template:
      transition: "{{ duration | default (3) }}"
      color_temp: "{{ colorValue }}"
      brightness: "{{ brightnessLevel | default(255) }}"
      entity_id: "{{ entity }}"